using UnityEditor;

namespace DialogSystem.EditorTools.Settings.Panels
{
    /// <summary>
    /// Intentionally left empty while the input settings UI is hidden from the product.
    /// </summary>
    internal sealed class InputPanel : BasePanel
    {
        public override void BuildUI(SerializedObject masterSo)
        {
        }
    }
}
